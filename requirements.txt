pyserial>=3.5
# Official Feetech servo SDK. Installs the `scservo_sdk` Python package used by
# servo_positions.py (PortHandler, sms_sts, ...).
ftservo-python-sdk==2.0.0
# Optional: only used to round the corners of the buttons in the app.
# Without it the app runs with square buttons.
Pillow>=9.0
